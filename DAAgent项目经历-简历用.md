# Agent 项目经历（简历用）

> 依据 `ARCHITECTURE.md` 与 `docs/PRD.md`。技术叙事以 **Agent 架构与运行时** 为主；RAG 仅作为 Agent 工具链中的一环，不单独展开成产品故事。

---

## 多工具编排的业务分析 Agent（DAAgent）

### 项目背景

复杂业务任务（对齐口径、多源交叉验证、数据分析、产出报告）需要多步决策与多工具协同，纯 LLM 问答与固定管线式 RAG 都不够：前者易幻觉、不可控；后者流程写死——检索一次就生成，不会判断信息是否充分，也不会在检索之外调用分析工具。

根因是缺一个 **能自主规划、自主选工具、自主判断何时停** 的 Agent 运行时。项目以 Agent 为系统主控：围绕任务目标做多轮决策，按需调度知识检索（RAG）、数据画像、沙箱代码执行、建模、报告导出等工具，最终交付可追溯结果。

### 我做了什么

**1. Agent 决策架构：Loop / Harness 分离**

- 设计「大脑—身体」边界：
  - **`agent/loop`（决策）**：组装 Prompt（系统指令 + 对话/任务状态 + 工具列表）→ 调 LLM → 解析输出为 `Action(tool, args)` 或 `Finish(answer)`，**不执行**。
  - **`runtime/harness`（执行）**：权限检查、超时、沙箱/隔离调用、错误捕获与标准化，返回 `Observation(result, error, duration)` 并记 trace。
- 驱动循环：`while not done: action = loop.step(state); observation = harness.execute(action); state.append(observation)`。
- 产品/工程收益：决策逻辑与执行治理解耦，新工具只注册不改大脑；超时、权限、失败策略统一在 Harness 治理，便于排障与扩展。

**2. ReAct 多轮推理 + 问题分解 Planner**

- **ReAct 作为 loop 内思考策略**（Thought → Action → Observation），支撑「想一步、做一步、看结果再决策」，而非一次输出终答。
- 设计 **planner**：复杂问题自动拆为多跳子问题序列，Agent 按计划逐步求解，支持跨来源交叉验证（如「文档 A 口径 + 文档 B 历史结论 + 当前数据表 → 综合判断」）。
- **信息充分性自评**：Agent 判断「当前证据够不够」——不够则改写查询再检索、换工具或请求人工澄清；足够则进入下一阶段或 Finish。检索次数与策略由 Agent 运行时决定，不写死在管线里。

**3. 工具体系与编排（RAG 为其中一环）**

- 设计工具注册与发现（registry + schema/描述），Agent 按任务自主选择：
  - **检索工具（RAG）**：查询改写 → 混合召回（BGE 稠密 + BM25 稀疏 + RRF）→ 上下文压缩 → 返回片段与引用；仅作为知识获取手段。
  - **数据工具**：加载、画像、探索。
  - **代码工具**：LLM 生成 Python，**隔离子进程沙箱执行**，拒绝 NaN/Inf，结果可复现。
  - **建模 / 可视化 / 导出**：按需包装现有模块，产出 Markdown/DOCX 报告。
- 明确边界：`agent/` 只产 Action 不直接执行；执行侧统一走 harness + sandbox，保证安全与可审计。

**4. 状态、质量门与人机协同（自主 ≠ 失控）**

- Agent 状态机：对话/任务历史 + 已检索片段 + 工具结果 + 中间结论（working memory），支撑多轮一致性。
- 设计三道质量门作为 Agent 流程硬约束：
  - **G0 摄入门**：问题/数据信息不足 → 澄清（主键、时间范围、成功指标），可终止或补材料；
  - **GQ 逐问门**：按子问题校验（样本量、显著性、业务合理性等），不通过可重试；
  - **GF 交付门**：报告自动审查，不过则修订或带风险标注强制交付。
- 子问题**预算控制**、任务取消、断点续跑（检查点），避免 Agent 失控烧 token 或跑飞。
- SSE 节点级进度推送，用户可实时看到 Agent 在哪一 Phase、在调什么工具。

**5. 运行时周边与可观测**

- 规划/对齐分层：`agent`（决策）/ `runtime`（执行与沙箱）/ `tools`（能力注册）/ `rag`（检索基建，被工具调用）/ `gateway`（模型路由、降级、缓存、限流、计量）/ `memory`（工作/短期/长期/情景记忆）/ `observability` + `security` + `evaluation` 横切。
- 结构化日志（`run.log`：节点耗时、token、工具调用）+ 检查点目录，支持复现与排查。
- 评测规划：任务级回归、检索质量（Recall@K/MRR/NDCG）、回答忠实度——用指标约束 Agent 行为迭代，而不是靠感觉改 prompt。

**6. 需求与落地**

- 撰写 PRD：目标用户、三条使用路径、FR/NFR、成功标准（完成率 ≥85%、平均耗时 ≤15min、满意度 ≥4/5）。
- 与研发对齐架构边界与非功能需求（提交到首次 SSE ≤2s、单子问题 ≤8min、检索 ≤3s、路径安全与上传限制）。
- 交付 FastAPI + React 前后端，形成可运行的 Agent 任务面板与知识库管理界面。

### 效果产出

- 交付 **以 Agent 为主控的多工具编排平台**：围绕业务问题自主规划，多轮调度检索、数据分析、沙箱代码与建模工具，完成从问题到可交付报告的闭环。
- Agent 运行时具备：决策/执行解耦、ReAct 多轮推理、问题分解、充分性自评、预算控制、质量门、检查点续跑、SSE 进度、结构化日志——**可监控、可干预、可复现**。
- RAG 作为工具链一环被统一编排：混合检索 + 引用溯源，支撑多跳知识获取，但不构成系统中心。
- 沉淀可迁移的 Agent 产品方法论：主控与工具边界、自主与兜底平衡、评测约束行为——可复用于企业知识助手、智能分析、NL2SQL 等场景。

---

## 简历一句话版

**多工具编排的业务分析 Agent（DAAgent）**  
以 Agent 为决策主控的智能体运行时：Loop/Harness 决策执行分离，ReAct 多轮推理 + Planner 问题分解，自主选择并调度 RAG 检索、数据画像、沙箱代码执行、建模与报告导出等工具；G0/GQ/GF 三道质量门 + 预算控制 + 人工澄清保证可控；检查点续跑与结构化日志支持可复现。完成 PRD 与分层架构，交付 FastAPI + React 可运行平台，实现「问题理解 → 多工具求解 → 可追溯交付」闭环。

---

## 面试可讲的技术要点（Agent 向）

1. **为什么 Loop/Harness 分离**：LLM 只决定「做什么 Action」，执行侧统一做权限、超时、沙箱与错误标准化——大脑可替换、身体可治理，新工具注册即可接入。
2. **ReAct + 充分性自评解决什么**：复杂问题不能一次检索定生死；Agent 多轮「查—评—改写—再查或换工具」，把「流程写死」变成「策略动态」。
3. **质量门是 Agent 的产品级护栏**：G0 信息不够先澄清，GQ 按题型校验，GF 交付前审查——ToB 要的是可控自主，不是全自动黑盒；预算与检查点保证可运营。
