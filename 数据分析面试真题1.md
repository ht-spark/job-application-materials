# 数据分析求职面试题 - 完整答案汇总

> 来源：面试资料1.pdf
> 整理日期：2026-05-25
> 优化日期：2026-05-25（SQL 校验修正 + 面试官视角补强）

---

## 目录

1. [快手 - 数据分析（国际化部门）](#一快手---数据分析国际化部门)
2. [快手 - 国际化部门（英文面试）](#二快手---国际化部门英文面试)
3. [哈啰 - 数据分析（用户增长方向）](#三哈啰---数据分析用户增长方向)
4. [字节跳动 - 国际化部门](#四字节跳动---国际化部门)
5. [字节跳动 - Seed PM日常实习](#五字节跳动---seed-pm日常实习)
6. [百度 - 文心一言 - 大模型产品经理](#六百度---文心一言---大模型产品经理)
7. [Bilibili - 数据分析（流量生态组）](#七bilibili---数据分析流量生态组)
8. [滴滴 - 数据科学（国际化部门）](#八滴滴---数据科学国际化部门)

---

## 答题总原则（面试官最看重）

1. **先定义再算**：15 秒说清指标分母、时间窗口、去重键、用户范围
2. **方法带假设**：说明用的是相关还是因果，关键假设是什么
3. **主动打边界**：并列、空集、除零、幸存者偏差、时区
4. **收口到动作**：结论能指导投放/产品/实验，而不是只报数

---

## 一、快手 - 数据分析（国际化部门）

### 笔试题

#### SQL 题1：留存率计算

**题目：** 给出每日活跃设备表，输出新设备数、第几日留存、留存设备数、留存率（直至第30日）

**口径先说清：**

- 新设备 = **观测窗口内首次活跃日期**（不是产品注册新增；窗口前已存在的设备会被判为新设备，面试时点明）
- 第 N 日留存 = 新增日起第 N 天仍活跃（N=1..30，当天为第 0 日不计入留存）
- 0 留存的日期也要输出（用 1..30 数字维表补全），否则表不完整

**答案：**

```sql
-- 假设表结构: daily_active(device_id, active_date)
-- MySQL 8.0 / 有窗口函数

WITH new_devices AS (
    -- 每个设备的新增日期 = 窗口内首次活跃
    SELECT 
        device_id,
        MIN(active_date) AS new_date
    FROM daily_active
    GROUP BY device_id
),
daily_new AS (
    SELECT 
        new_date,
        COUNT(DISTINCT device_id) AS new_device_count
    FROM new_devices
    GROUP BY new_date
),
-- 1..30 日留存编号维表（保证 0 留存也出行）
retention_days AS (
    SELECT 1 AS retention_day
    UNION ALL SELECT 2  UNION ALL SELECT 3  UNION ALL SELECT 4
    UNION ALL SELECT 5  UNION ALL SELECT 6  UNION ALL SELECT 7
    UNION ALL SELECT 8  UNION ALL SELECT 9  UNION ALL SELECT 10
    UNION ALL SELECT 11 UNION ALL SELECT 12 UNION ALL SELECT 13
    UNION ALL SELECT 14 UNION ALL SELECT 15 UNION ALL SELECT 16
    UNION ALL SELECT 17 UNION ALL SELECT 18 UNION ALL SELECT 19
    UNION ALL SELECT 20 UNION ALL SELECT 21 UNION ALL SELECT 22
    UNION ALL SELECT 23 UNION ALL SELECT 24 UNION ALL SELECT 25
    UNION ALL SELECT 26 UNION ALL SELECT 27 UNION ALL SELECT 28
    UNION ALL SELECT 29 UNION ALL SELECT 30
),
-- 设备在「新增后第 N 日」是否活跃
retention_detail AS (
    SELECT 
        n.new_date,
        DATEDIFF(d.active_date, n.new_date) AS retention_day,
        COUNT(DISTINCT n.device_id) AS retention_device_count
    FROM new_devices n
    JOIN daily_active d
        ON n.device_id = d.device_id
       AND DATEDIFF(d.active_date, n.new_date) BETWEEN 1 AND 30
    GROUP BY n.new_date, DATEDIFF(d.active_date, n.new_date)
)
SELECT 
    dn.new_date AS `新增日期`,
    dn.new_device_count AS `新设备数量`,
    rd.retention_day AS `第几日留存`,
    COALESCE(r.retention_device_count, 0) AS `留存设备数`,
    CONCAT(
        ROUND(COALESCE(r.retention_device_count, 0) * 100.0 / dn.new_device_count, 2),
        '%'
    ) AS `留存率`
FROM daily_new dn
CROSS JOIN retention_days rd
LEFT JOIN retention_detail r
    ON r.new_date = dn.new_date
   AND r.retention_day = rd.retention_day
ORDER BY dn.new_date, rd.retention_day;
```

**边界与可追问点：**

- 0 留存日：`LEFT JOIN + COALESCE` 保证出行
- 若表有重复活跃记录：已用 `COUNT(DISTINCT device_id)`
- 时区/业务日切分：国际化业务需统一到同一业务时区再算 `active_date`
- 性能：设备量大时可先按 `new_date` 分区跑，或物化 `new_devices`

---

#### SQL 题2：积分奖品统计

**题目：** 给出用户积分增减变化表，根据每个用户历史积分的**最高值**，达到10获得奖品A，30获得A+B，80获得A+B+C，求三种奖品各需多少。

**口径先说清：**

- 「历史最高」= **累计积分余额的峰值**（可增可减），不是单次获得最大值
- 奖品是**叠加**的：峰值 ≥80 同时领 A、B、C；≥30 领 A、B；≥10 领 A
- 同一天多笔变动：按 `change_date` 累加即可；若同毫秒乱序，可加自增流水号排序

**答案：**

```sql
-- 假设表结构: points_change(user_id, change_date, point_change)
-- 先算逐笔累计余额，再取每个用户的历史峰值

WITH running AS (
    SELECT 
        user_id,
        change_date,
        SUM(point_change) OVER (
            PARTITION BY user_id 
            ORDER BY change_date 
            ROWS UNBOUNDED PRECEDING
        ) AS running_pts
    FROM points_change
),
user_max AS (
    SELECT 
        user_id, 
        MAX(running_pts) AS max_hist_pts
    FROM running
    GROUP BY user_id
)
SELECT
    SUM(CASE WHEN max_hist_pts >= 10 THEN 1 ELSE 0 END) AS `奖品A数量`,
    SUM(CASE WHEN max_hist_pts >= 30 THEN 1 ELSE 0 END) AS `奖品B数量`,
    SUM(CASE WHEN max_hist_pts >= 80 THEN 1 ELSE 0 END) AS `奖品C数量`
FROM user_max;
```

**若要同时看每人领了哪些奖：**

```sql
SELECT 
    user_id,
    max_hist_pts,
    CASE 
        WHEN max_hist_pts >= 80 THEN 'A+B+C'
        WHEN max_hist_pts >= 30 THEN 'A+B'
        WHEN max_hist_pts >= 10 THEN 'A'
        ELSE '无'
    END AS prize
FROM user_max;
```

**边界：**

- 峰值可能是负数：`max_hist_pts < 10` 全部无奖
- 只关心「是否达到过」：用 `MAX(累计)`，即使后来花光也保留奖品资格

---

#### 情景题：分析用户六种行为对后续观看时长的提升最大

**题目：** 点赞、搜索、关注、评论、分享、收藏，哪种对后续观看时长提升最大？

**面试官期望的完整答法（相关 → 准因果 → 因果）：**

**1. 先定口径（必说）**

- 行为：T 日发生该行为（次数或 0/1）
- 结果：T+1 ~ T+7 的日均观看时长（或 T+1 单日，需选定后固定）
- 样本：近 3 个月有活跃的用户；排除机器人/灰产
- **明确这是「提升」= 因果问题，不能直接用相关系数下结论**

**2. 描述性分析（快速基线）**

- 对每个行为做有/无分组，看后续 7 日时长差
- 必须按**历史活跃度/历史时长**分层，否则高活用户既爱互动又爱看视频，会夸大所有行为

**3. 多元模型（控混淆后的相关）**

- 因变量：7 日日均观看时长
- 自变量：六种行为次数（可标准化后比系数）+ 控制变量（历史 7 日时长、活跃天数、粉丝数、设备、渠道…）
- 模型：`Y = β₀ + β₁X₁ + … + β₆X₆ + 控制变量 + ε`
- 比较：标准化回归系数 / 边际效应，并报置信区间

**4. 更接近因果（加分项）**

- **倾向得分匹配（PSM）**：把「发生过某行为」当处理，匹配相似用户再比时长
- **事件研究**：行为发生日前后时长趋势，看是否出现跳升
- **黄金标准 A/B**：随机引导「收藏 / 关注」等，看 T+7 时长是否上升——只有实验证明的才能叫「提升」

**5. 收口到业务动作**

> 若只能推一个：优先 A/B 测「引导收藏」或「引导关注」（通常预测力强、产品改动成本低）。上线前用匹配结果排序候选行为，上线后以时长与长期留存共同决策。
> 汇报时写清：模型给的是**预测力排序**，**提升幅度**以实验为准。

**常见追问：**

- 为什么不用随机森林特征重要性？→ 可补充，但仍是相关；对共线行为不稳定
- 行为组合效应？→ 加交互项或看「多行为用户」分层
- 反向因果？→ 爱看视频的人更爱点赞；用行为**当日之后**的结果窗口 + 匹配/实验缓解

---

### 面试题

#### Q1：K-means 中 K=3 如何确定？

**A：**

- **肘部法则（Elbow）**：画 K-SSE 曲线，取拐点；拐点不明显时结合业务
- **轮廓系数（Silhouette）**：不同 K 取最高分（通常比纯肘部更稳）
- **业务常识**：用户分层常对应高/中/低价值等可行动作的分层
- **稳定性**：多次随机初始化看分群是否稳定

**面试加分：** 「K=3」往往不是纯统计结论，而是「分完能对应 3 套运营策略」。

---

#### Q2：分类变量与连续变量预处理？one-hot 导致特征过多怎么办？

**A：**

- **连续变量**：树模型可不标准化；线性/距离模型需 Z-score 或 Min-Max
- **分类变量**：
  - 低基数：one-hot
  - 高基数：目标编码（Target Encoding，注意过拟合，宜在 CV 内计算）、频数编码、Embedding
- **one-hot 维度爆炸：**
  - 删低频类别或归为「其他」
  - 目标/频数/WOE 编码
  - 树模型直接用类别编码
  - PCA 不适合直接压 one-hot 语义，可作兜底

---

#### Q3：量化实习为何用随机森林而非 LSTM？

**A：**

- **任务是多因子合成/横截面预测**，不是纯时序预测 → 树模型更贴切
- **随机森林**：不易过拟合（相对）、可输出特征重要性、训练快、对缺失和尺度不敏感
- **LSTM**：适合有序时序，但金融序列噪声大、样本少时易过拟合，解释性差
- **实验对比（若有经历）**：同一特征集上 RF 的 IC/分层收益 vs LSTM，用样本外表现定模型

---

#### Q4：大数据库管理时，建表、连接等优化方法？

**A：**

- **建表**：合适数据类型、主键与索引、按时间/业务维度分区、必要时分库分表
- **查询**：避免 `SELECT *`、`EXPLAIN` 看执行计划、覆盖/联合索引、谓词下推
- **JOIN**：小表驱动大表、关联键类型一致并有索引、避免无必要的多表连、大表大表可考虑 MapJoin/分桶
- **面试加分：** 提到「先看慢查询日志/执行计划，再改写 SQL 或加索引，而不是盲目加索引」

---

## 二、快手 - 国际化部门（英文面试）

### 笔试题

#### SQL：连续10天发布视频且视频类型≥2的创作者

**题目：** 创作者视频发布表（creator_id, video_id, post_date, video_type），求连续10天发布视频且视频类型 ≥ 2 的创作者。

**口径先确认（歧义题必问）：**

- **解读 A（本答案）**：连续 10 个自然日都发布，且**每一天**视频类型数 ≥ 2

**思路：**

1.查每天种类大于2的

2.根据上述筛选后的表利用data_sub求 date_group

3.按创作者和date_group分组，然后聚合count、min、max，筛选count>10

**答案（解读 A）：**

```sql
-- 假设表结构: video_posts(creator_id, video_id, post_date, video_type)
WITH daily_posts AS (
    -- 每个创作者每天：类型数 ≥ 2 的「合格日」
    SELECT 
        creator_id,
        post_date,
        COUNT(DISTINCT video_type) AS type_count
    FROM video_posts
    GROUP BY creator_id, post_date
    HAVING COUNT(DISTINCT video_type) >= 2
),
consecutive_dates AS (
    -- 日期减行号 → 同一连续段共享同一 date_group
    SELECT 
        creator_id,
        post_date,
        DATE_SUB(post_date, INTERVAL 
            ROW_NUMBER() OVER (PARTITION BY creator_id ORDER BY post_date) DAY
        ) AS date_group
    FROM daily_posts
),
consecutive_stats AS (
    SELECT 
        creator_id,
        date_group,
        COUNT(*) AS consecutive_days,
        MIN(post_date) AS start_date,
        MAX(post_date) AS end_date
    FROM consecutive_dates
    GROUP BY creator_id, date_group
)
SELECT DISTINCT creator_id
FROM consecutive_stats
WHERE consecutive_days >= 10;
```

---

## 三、哈啰 - 数据分析（用户增长方向）

### 面试题

#### Q1：次日留存率 SQL

**口径先说清：**

- **新用户次留**：分母 = 当日新增（首活/注册=当日）

1.查每天的新增加用户，也就是每个用户的min(date)

2.关联原始表，利用新注册日期与活跃时间取差值为1得到每个新增日期是次日留存人数

3.最后用首登日期分组，然后count相除计算留存率

**答案（新用户次留，更常考）：**

```sql
WITH first_login AS (
    SELECT user_id, MIN(login_date) AS register_date
    FROM user_login
    GROUP BY user_id
)
SELECT 
    f.register_date AS `新增日期`,
    COUNT(DISTINCT f.user_id) AS `新增用户数`,
    COUNT(DISTINCT t2.user_id) AS `次日留存用户数`,
    ROUND(COUNT(DISTINCT t2.user_id) * 100.0 / NULLIF(COUNT(DISTINCT f.user_id), 0), 2) AS `次日留存率%`
FROM first_login f
LEFT JOIN user_login t2
    ON t2.user_id = f.user_id
   AND DATEDIFF(t2.login_date, f.register_date) = 1
GROUP BY f.register_date
ORDER BY f.register_date;
```

**边界：** 同日多条登录用 `DISTINCT`；`NULLIF` 防除零；最后一天 cohort 天然次留未成熟，报表需标灰。

---

#### Q2：成交总金额最大的用户

1.先按用户分组聚合求每个用户的总成交金额

2.利用窗口函数对总成交金额排序

3.where ranking=1即可

**答案：**

```sql
-- 假设表: orders(user_id, order_amount, order_date)
WITH user_total AS (
    SELECT
        user_id,
        SUM(order_amount) AS total_amount
    FROM orders
    GROUP BY user_id
),
ranked AS (
    SELECT
        user_id,
        total_amount,
        RANK() OVER (ORDER BY total_amount DESC) AS rk
    FROM user_total
)
SELECT
    user_id,
    total_amount
FROM ranked
WHERE rk = 1;
```

**边界：** 是否过滤退款/取消单？是否含 0 元单？口径先问。

---

#### Q3：最大连续登录天数

**答案：**

```sql
WITH ranked_logins AS (
    SELECT 
        user_id,
        login_date,
        DATE_SUB(login_date, INTERVAL 
            ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY login_date) DAY
        ) AS date_group
    FROM (SELECT DISTINCT user_id, login_date FROM user_login) t
),
consecutive_counts AS (
    SELECT 
        user_id,
        date_group,
        COUNT(*) AS consecutive_days
    FROM ranked_logins
    GROUP BY user_id, date_group
)
SELECT user_id, MAX(consecutive_days) AS max_consecutive_days
FROM consecutive_counts
GROUP BY user_id;
```

**要点：** 先 `DISTINCT` 再打组；`DATE_SUB + ROW_NUMBER` 是连续日期标准套路。

---

#### Q4：SQL 执行顺序

**A：**

```
FROM → JOIN (ON) → WHERE → GROUP BY → HAVING → WINDOW → SELECT → DISTINCT → ORDER BY → LIMIT/OFFSET
```

口语可举：`SELECT` 里的别名不能在 `WHERE` 用，但能在 `ORDER BY` 用，因为 `SELECT` 先于 `ORDER BY` 生效。

---

#### Q5：三种排序函数区别（rank、dense_rank、row_number）

**A：**

| 函数       | 相同值处理   | 跳跃 | 示例(1,1,3) |
| ---------- | ------------ | ---- | ----------- |
| row_number | 顺序编号     | 无   | 1,2,3       |
| rank       | 相同值同排名 | 有   | 1,1,3       |
| dense_rank | 相同值同排名 | 无   | 1,1,2       |

**业务选型：** 只要 1 条 Top → `ROW_NUMBER`；并列都要 → `RANK/DENSE_RANK`；去最高最低薪要「整档去掉」→ 不要用 `ROW_NUMBER`，用 `MIN/MAX` 窗口过滤。

---

#### Q6：中心极限定理

**A：**

- **定义**：样本量足够大时，样本均值的抽样分布近似正态，不依赖总体分布形状
- **意义**：大样本均值检验、置信区间的理论基础
- **业务例子**：单用户观看时长是长尾，但人均时长可近似用 t 检验做 AB 对比
- **边界**：极端厚尾/强依赖（未独立抽样）时收敛慢，需 bootstrap

---

#### Q7：p 值的含义，p=0.05 代表什么

**A：**

- **p 值**：原假设为真时，出现当前或更极端样本结果的概率
- **p=0.05**：若无效（原假设真），仍有 5% 机会看到这么极端的结果
- **常见误读（要说出来）**：p 值**不是**「原假设为真的概率」，也**不是**「效应大小」
- **决策**：常以 α=0.05 为阈；但增长实验还看效应量、置信区间、业务成本

---

#### Q8：第一类错误与第二类错误

**A：**

| 类型         | 名称        | 含义                             | 概率 |
| ------------ | ----------- | -------------------------------- | ---- |
| 第一类错误   | α / 假阳性 | 原假设为真却拒绝（无效当有效）   | α   |
| 第二类错误   | β / 假阴性 | 原假设为假却未拒绝（有效当无效） | β   |
| 检验力 Power | 1-β        | 正确检出真实效应                 | 1-β |

**业务例子：** 上线一个无效策略 = 假阳性，浪费研发；错过有效策略 = 假阴性，损失增长。高频策略实验通常更控假阳性。

---

#### Q9：Xgboost 原理

**A：**

- **Boosting**：串行加树，每棵树拟合当前损失的负梯度（二阶展开）
- **目标**：`Obj = ΣL(yi, ŷi) + ΣΩ(fk)`（损失 + 正则）
- **正则**：`Ω(f) = γT + ½λΣw²`（叶子数 + 叶子权重 L2）
- **分裂**：贪心选增益最大分裂点；支持缺失值方向学习
- **相对 GBDT：** 二阶梯度、正则化、列采样、工程高效

---

#### Q10：Xgboost 防过拟合措施

**A：**

- 正则：`reg_alpha` / `reg_lambda`
- 限制复杂度：`max_depth`、`min_child_weight`、`gamma`
- 采样：`subsample`、`colsample_bytree`
- 收缩：较小的 `learning_rate` + 更多树
- `early_stopping_rounds` 以验证集停训

---

## 四、字节跳动 - 国际化部门笔试题

与快手国际化部门题目相同，见上文。

---

## 五、字节跳动 - Seed PM 日常实习

### 面试题

#### Q1：大模型训练过程

**A：**

1. **预训练**：大规模无标注语料，自监督学习通用表示
2. **SFT**：高质量指令-回答对，对齐任务格式与帮助性
3. **偏好对齐**：RLHF / DPO 等，按人类偏好优化
4. **后训练**：领域适配、持续学习、安全加固

---

#### Q2：高质量数据的特征

**A：**

- **准确**：事实正确、逻辑自洽
- **多样**：领域、任务、语言、表达方式覆盖
- **有信息量/复杂**：含多步推理、工具使用等
- **格式规范**：结构清晰、标注一致
- **去重**：减少记忆与过拟合
- **安全**：过滤有害、偏见、隐私

---

#### Q3：如何大批量筛选高复杂度数据（除人工外）

**A：**

- **模型打分**：强模型按 rubric 打复杂度 1–5 分
- **规则粗筛**：长度、步骤数、是否含「证明/对比/多跳」等信号
- **困惑度/模型分歧**：多模型意见不一致的样本往往更有信息量（作补充信号）
- **抽样校准**：小样本人工对齐自动分，再放量

示例 prompt：

```
请评估以下问题的复杂度（1-5分）：
1分：简单事实查询
2分：单步推理
3分：多步推理
4分：需要领域知识的多步推理
5分：需要创造性思维或深度分析

问题：[问题内容]
输出：复杂度评分；理由（一句话）
```

---

#### Q4：COT 过长问题如何压缩（SFT 和 RL 分别怎么实现）

**A：**

**SFT：**

- 构造「正确且简洁」的推理轨迹作目标
- 知识蒸馏：用长 CoT 教师生成，再人工/规则压缩成短 CoT 监督学生
- 指令模板要求「先给结论，推理不超过 N 步」

**RL：**

- 奖励 = 正确性 − 长度惩罚（或超预算后阶梯惩罚）
- 约束最大思考长度 / token 预算
- 可对比：仅惩罚长度会「偷工减料」，需保正确率不降

---

#### Q5：如何人工标注 CoT

**A：**

1. **标签体系**：每一步必要性（必须/可简化/可删）、正确性、冗余度
2. **流程**：模型先生成 → 标注逐步裁剪/重写 → 得到「短而对」的标准 CoT
3. **质检**：多人一致率、专家抽检、争议仲裁

---

#### Q6：如何计算抖音用户的 365 天 LTV（存在注册不足 365 天的用户）

**口径先说清：**

- LTV = 用户生命周期内带来的**收入**（或毛利）；此处为 365 日累计
- 未满 365 天不能直接加总，需**外推/同期群估计**
- 必须含**未付费用户**，否则是条件 LTV，会偏高

**面试标准答法（方法优先于一条 SQL）：**

1. **同期群（Cohort）曲线法（推荐）**
   - 用已满 365 天的成熟注册 cohort，计算累计 LTV(d)
   - 得到比例 `k_d = LTV(d)/LTV(365)`（如 30 日、90 日）
   - 新 cohort：`LTV(365) ≈ LTV(d)/k_d`
2. **参数曲线**：如 `LTV(d)=LTV∞·(1-e^{-λd})` 拟合后外推到 365
3. **线性外推** `收入×365/已注册天数`：仅粗估，**会高估**（LTV 曲线通常前高后低），需注明

**参考 SQL（成熟 cohort 比例法骨架）：**

```sql
-- user_info(user_id, register_date)
-- user_transactions(user_id, pay_date, revenue)  -- 含退款时用净收入

WITH pay AS (
    SELECT 
        i.user_id,
        i.register_date,
        DATEDIFF(t.pay_date, i.register_date) AS age_day,
        t.revenue
    FROM user_info i
    LEFT JOIN user_transactions t ON i.user_id = t.user_id
    WHERE i.register_date >= DATE_SUB(CURRENT_DATE, INTERVAL 365 DAY)
),
-- 注意 LEFT JOIN：无付费用户 revenue 为 NULL/0，必须保留
user_age_rev AS (
    SELECT 
        user_id,
        register_date,
        MAX(age_day) AS observed_days,          -- 观测到的年龄
        SUM(COALESCE(revenue, 0)) AS rev_to_date
    FROM pay
    GROUP BY user_id, register_date
),
mature_curve AS (
    -- 历史成熟用户：各 age_day 的累计收入占比（示意，实际需老 cohort 明细）
    -- k_30 = LTV(30)/LTV(365), k_90 = LTV(90)/LTV(365)
    SELECT 30 AS age_day, 0.55 AS k_ratio
    UNION ALL SELECT 90, 0.78
    UNION ALL SELECT 180, 0.90
)
SELECT 
    u.register_date,
    AVG(
        CASE 
            WHEN u.observed_days >= 365 THEN u.rev_to_date
            ELSE u.rev_to_date / m.k_ratio   -- 用最近的成熟比例回推
        END
    ) AS est_ltv_365
FROM user_age_rev u
LEFT JOIN mature_curve m
    ON m.age_day = (
        SELECT MAX(m2.age_day) 
        FROM mature_curve m2 
        WHERE m2.age_day <= u.observed_days
    )
GROUP BY u.register_date;
```

**边界：** 注册 0 天除零、退款、币种/汇率、时区；向面试官强调「外推假设比公式更重要」。

---

## 六、百度 - 文心一言 - 大模型产品经理

### 面试题

#### Q1：NL2SQL 评估指标

**A：**

- **执行准确率（Execution Accuracy）**：执行结果与金标一致的比例（主指标）
- **组件/逻辑匹配**：SELECT 谓词、JOIN、聚合等子句匹配（可解释错误来源）
- **测试套件准确率**：多个扰动库上都对，减少「碰巧执行对」
- **工程指标**：延迟、失败率、用户改写率
- **验证方法**：固定题库 + 执行对比；人工抽查看歧义题

---

#### Q2：内部数据库格式不一致怎么处理

**A：**

- 统一命名与类型规范，建**数据字典**
- 映射层/中间层做字段与口径转换
- 元数据管理记录源表结构与血缘
- ETL 增量同步 + 质量校验（空值率、主键唯一、波动）
- NL2SQL 场景：把多源 schema **逻辑统一**成业务语义层，降低模型直接面对脏 schema

---

#### Q3：产品经理需要的能力

**A：**

1. 业务理解与优先级
2. 用户洞察（痛点/痒点、可验证假设）
3. 技术边界感（能与研发对齐方案）
4. 数据分析（定义指标、读实验）
5. 推动落地与跨团队协作

（结合岗位补一条：LLM PM 还要懂评测、数据生产、安全合规。）

---

#### Q4：国内外主流大模型优缺点对比

**A：**（产品向对比，具体版本会变，答结构更重要）

| 模型                       | 优点                   | 缺点/注意          |
| -------------------------- | ---------------------- | ------------------ |
| GPT-4/4o 系                | 综合与工具调用强       | 成本、延迟         |
| Claude                     | 长上下文、代码与写作稳 | 中文场景需实测     |
| Gemini                     | 多模态与长上下文       | 产品碎片化         |
| 文心/通义/豆包/DeepSeek 等 | 中文与本地生态、合规   | 垂直任务需自建评测 |

**答题套路：** 不背参数表，而是「场景 → 评测集 → 成本/延迟/安全」选型。

---

#### Q5：如何写 deep research 的 prompt

**A：**

```
角色：你是[领域]资深分析师/研究者。

目标：回答[具体问题]，面向[读者]做决策。

过程要求：
1. 检索并交叉验证高质量来源（论文/行业报告/官方数据），标注出处与时间
2. 先给执行摘要，再分主题展开；区分事实与推断
3. 列出观点分歧与数据局限
4. 给出可执行建议与下一步验证方式

输出结构：
- 执行摘要（≤200字）
- 关键发现（带证据）
- 分析正文
- 争议与局限
- 建议
- 参考来源
```

**加分：** 要求「先列检索计划再检索」，避免一上来长篇幻觉。

---

#### Q6：用户对大模型的 query 类型

**A：**

- 创作：写作、文案、改写
- 问答/学习：概念、辅导、解释
- 角色与陪伴：人设、闲聊
- 工具：代码、翻译、表格/数据分析
- 决策咨询：对比方案、建议
- （可补）多模态理解、Agent 任务型指令

**PM 加分：** 分类要能落到评测集、功能优先级和安全策略。

---

## 七、Bilibili - 数据分析（流量生态组）

### 面试题

#### SQL 题1：每个视频分区内，播放时长最高的视频的播放量

**口径：** 「播放时长最高」并列时是否都要？默认**都要**更稳妥。

```sql
1.按类别通过窗口函数对播放时长排序
2.取播放时长最长的也就是rank=1的视频
```

**答案：**

```sql
-- 假设表: videos(video_id, category, play_duration, play_count)
WITH ranked_videos AS (
    SELECT 
        video_id,
        category,
        play_duration,
        play_count,
        DENSE_RANK() OVER (
            PARTITION BY category 
            ORDER BY play_duration DESC
        ) AS rn
    FROM videos
)
SELECT 
    category AS `分区`,
    video_id AS `视频ID`,
    play_duration AS `播放时长`,
    play_count AS `播放量`
FROM ranked_videos
WHERE rn = 1;
```

**只要一条时：** 把 `DENSE_RANK` 换成 `ROW_NUMBER`。
**边界：** `play_duration` 是总时长还是篇均？口径先问。

---

#### SQL 题2：将一列如"语文：100；数学：90"拆分成两列

**答案：**

```sql
-- MySQL：固定两组「科目：分数」
SELECT 
    SUBSTRING_INDEX(SUBSTRING_INDEX(subject_scores, '；', 1), '：', 1) AS subject1,
    SUBSTRING_INDEX(SUBSTRING_INDEX(subject_scores, '；', 1), '：', -1) AS score1,
    SUBSTRING_INDEX(SUBSTRING_INDEX(subject_scores, '；', -1), '：', 1) AS subject2,
    SUBSTRING_INDEX(SUBSTRING_INDEX(subject_scores, '；', -1), '：', -1) AS score2
FROM student_scores;
```

**多组通用（注意必须用 CHAR_LENGTH，不能用 LENGTH）：**

```sql
-- MySQL 8.0；LENGTH 是字节，SUBSTRING 按字符 —— 中文会错位
WITH RECURSIVE split AS (
    SELECT 
        id,
        subject_scores,
        SUBSTRING_INDEX(subject_scores, '；', 1) AS item,
        SUBSTRING(
            subject_scores, 
            CHAR_LENGTH(SUBSTRING_INDEX(subject_scores, '；', 1)) + 2
        ) AS remaining
    FROM student_scores
    UNION ALL
    SELECT 
        id,
        subject_scores,
        SUBSTRING_INDEX(remaining, '；', 1),
        CASE 
            WHEN LOCATE('；', remaining) > 0 
            THEN SUBSTRING(
                remaining, 
                CHAR_LENGTH(SUBSTRING_INDEX(remaining, '；', 1)) + 2
            )
            ELSE ''
        END
    FROM split
    WHERE remaining != ''
)
SELECT 
    id,
    SUBSTRING_INDEX(item, '：', 1) AS subject,
    CAST(SUBSTRING_INDEX(item, '：', -1) AS SIGNED) AS score
FROM split
WHERE item != '';
```

**边界：** 分隔符是中文全角 `；` `：`；分数是否非数字；只有 1 个科目时固定写法会得到怪值——通用写法更稳。

---

#### Q1：大数定律与中心极限定理

**A：**

**大数定律：** 试验次数增加，样本均值依概率收敛于总体均值。
应用：蒙特卡洛、用频率近似概率、精算稳定估计。

**中心极限定理：** 大样本下样本均值分布近似正态。
应用：AB 均值检验、置信区间。

**一句话区分：** 大数定律谈「收敛到哪」，中心极限谈「波动长什么样」。
**业务例子：** 看人均使用时长做 AB 可用均值检验，但极值用户多时要 winsorize 或 bootstrap。

---

#### Q2：识别灰黑产设备（利用点击广告注册获利）

**A：**

- **账号/IP/设备关联**：一机多号、一号多 IP、模拟器/root、设备指纹聚集
- **行为序列**：注册后秒点广告、路径单一、停留极短、无深度浏览
- **时间聚集**：批量注册、定时点击
- **转化异常**：广告点击率异常高但留存接近 0
- **落地方式：** 规则分层（硬规则 + 模型分）→ 人审抽样 → 回流标签迭代
- **边界：** 共享 IP（校园/公司）会误伤，需结合设备与行为，而不是单靠 IP

---

#### Q3：如何调整各渠道广告投放比例

**A：**

- **目标：** 在预算约束下最大化长期价值，而不是只看当日 CPI
- **核心公式：** `ROI = (增量 LTV - CAC) / CAC`，并看回本周期
- **指标层次：** 短期（CPI、注册、CPA）→ 中期（D3/D7/D30 留存）→ 长期（LTV、增量 lift）
- **策略：**
  - 高边际 ROI：加预算，但注意规模扩大后 ROI 递减
  - 低 CPI 低质：减量或改素材/定向
  - 新渠道：小预算 A/B 或地理实验
- **必须提：** 观测 ROI 混杂自然量，理想看**增量实验**或 MMM；渠道间人群重叠要控

---

#### Q4：用户登录日志，求每个用户最近登录日期

**答案：**

```sql
-- 最简：直接聚合
SELECT 
    user_id,
    MAX(login_date) AS last_login_date
FROM user_login
GROUP BY user_id;

-- 若还需带回最近一次登录的其它字段：ROW_NUMBER
SELECT user_id, login_date AS last_login_date
FROM (
    SELECT 
        user_id,
        login_date,
        ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY login_date DESC) AS rn
    FROM user_login
) t
WHERE rn = 1;
```

**三种 rank 区别同前文。** 这题用 `MAX` 即可，不必上窗口。

---

## 八、滴滴 - 数据科学（国际化部门）

### 面试题

#### SQL：去掉每部门最高和最低薪水后，求各部门平均薪水

**口径：** 并列最高/最低是「各去掉一人」还是「整档去掉」？下例为**整档去掉**（更常见）。

**答案（推荐）：**

```
1.先求各部门内工资的排序，通过dense_rank的窗口函数
2.通过where去掉最小和最大的员工
3.按照部门分组后聚合求平均工资
```

```sql
-- 假设表: employees(employee_id, department, salary)
WITH flagged AS (
    SELECT 
        department,
        employee_id,
        salary,
        MIN(salary) OVER (PARTITION BY department) AS dept_min,
        MAX(salary) OVER (PARTITION BY department) AS dept_max,
        COUNT(*) OVER (PARTITION BY department) AS dept_count
    FROM employees
)
SELECT 
    department,
    AVG(salary) AS avg_salary_except_min_max,
    COUNT(*) AS employee_count
FROM flagged
WHERE salary > dept_min 
  AND salary < dept_max
  AND dept_count >= 3   -- 1~2 人无法去掉两端
GROUP BY department;
```

**若题意是「各去掉一人（排名）」：** 用 `ROW_NUMBER` 按薪排序过滤 `rn_asc > 1 AND rn_desc > 1`（并列时只去一人）。

**边界：** 部门 1～2 人；全员同薪；空部门——面试官希望你主动约定返回 NULL 还是返回原均值。

---

#### Python 题1：二分查找

**答案：**

```python
def binary_search(arr, target):
    """
    在有序数组中查找目标值下标；不存在返回 -1
    时间复杂度 O(log n)，空间 O(1)
    """
    left, right = 0, len(arr) - 1

    while left <= right:
        mid = (left + right) // 2  # 大数组可写 left + (right - left) // 2

        if arr[mid] == target:
            return mid
        elif arr[mid] < target:
            left = mid + 1
        else:
            right = mid - 1

    return -1


if __name__ == "__main__":
    arr = [1, 3, 5, 7, 9, 11, 13]
    assert binary_search(arr, 7) == 3
    assert binary_search(arr, 4) == -1
    assert binary_search([], 1) == -1
```

---

#### Python 题2：两数之和（力扣 hot100 第一题）

**答案：**

```python
def two_sum(nums, target):
    """
    返回两数下标；无解返回 []
    时间 O(n)，空间 O(n)
    """
    num_dict = {}  # 值 -> 下标

    for i, num in enumerate(nums):
        complement = target - num
        if complement in num_dict:
            return [num_dict[complement], i]
        num_dict[num] = i

    return []


def two_sum_values(nums, target):
    """返回数字对而非下标（注意重复值）"""
    seen = set()
    for num in nums:
        complement = target - num
        if complement in seen:
            return [complement, num]
        seen.add(num)
    return []


if __name__ == "__main__":
    assert two_sum([2, 7, 11, 15], 9) == [0, 1]
    assert two_sum([3, 3], 6) == [0, 1]
```

---

#### Q1：向不懂统计的人解释 p-value

**A：**

> 「假设新药其实和旧药一样好（其实没效果）。p 值就是：在『没效果』的前提下，我们仍然看到数据像现在这么好的概率。
> 比如 p=0.03：如果药没用，只有 3% 的机会看到这么好的数据。这个机会很小，所以我们更愿意相信药真的有点用。
> 常用 0.05 当门槛，但 p 值不等于药有效的概率，效果有多大还要看提升幅度。」

---

#### Q2：多重共线性

**A：**

- **定义：** 特征之间高度线性相关
- **后果：** 系数方差大、符号不稳、难以解释单特征贡献；预测可能仍可接受
- **识别：** 相关矩阵、VIF
- **处理：** 删冗余特征、PCA/组合、L1/L2、收集更多数据
- **业务例子：** 「渠道花费」与「曝光量」共线时，回归可能给出错误的负系数

---

#### Q3：PCA（主成分分析）

**A：**

- **原理：** 正交线性变换，按方差最大方向提取主成分
- **步骤：** 标准化 → 协方差矩阵 → 特征值分解/奇异值分解 → 选前 k 个特征向量 → 投影
- **应用：** 降维、去噪、可视化
- **注意：** 对尺度敏感需标准化；主成分可解释性差；不能处理非线性流形（可用 t-SNE/UMAP/核 PCA）

---

#### Q4：L1 和 L2 正则化

**A：**

| 特性   | L1（Lasso）          | L2（Ridge）        |
| ------ | -------------------- | ------------------ |
| 公式   | λΣ\|w\|            | λΣw²            |
| 效果   | 稀疏，可做特征选择   | 权重收缩，不至过大 |
| 解     | 部分权重为 0         | 接近 0 但一般非 0  |
| 适用   | 特征多、要可解释子集 | 共线、防止过拟合   |
| 弹性网 | L1+L2 组合，实务常用 |                    |

---

#### Q5：SVM 原理

**A：**

- **思想：** 找最大间隔超平面分开两类
- **目标：** 最小化 `½‖w‖²`，约束 `yi(w·xi+b) ≥ 1`
- **支持向量：** 决定间隔的边界样本
- **核技巧：** 线性/多项式/RBF/Sigmoid，把线性不可分映射到高维
- **软间隔 C：** C 大更贴训练集（易过拟合），C 小更平滑

---

#### Q6：Xgboost 超参数调优

**A：**

```python
import xgboost as xgb
from sklearn.model_selection import GridSearchCV, RandomizedSearchCV

param_grid = {
    'max_depth': [3, 5, 7, 9],
    'learning_rate': [0.01, 0.1, 0.3],
    'n_estimators': [100, 200, 300],
    'subsample': [0.8, 0.9, 1.0],
    'colsample_bytree': [0.8, 0.9, 1.0],
    'reg_alpha': [0, 0.1, 1],
    'reg_lambda': [1, 2, 5],
}

model = xgb.XGBClassifier(eval_metric='logloss')
# 实际更常用 RandomizedSearchCV 或贝叶斯优化；树多时配 early_stopping
grid_search = GridSearchCV(
    model,
    param_grid,
    cv=5,
    scoring='roc_auc',
    n_jobs=-1,
)
grid_search.fit(X_train, y_train)
print(f"最佳参数: {grid_search.best_params_}")
print(f"最佳得分: {grid_search.best_score_}")
```

**实务顺序：** 先粗调 `max_depth/min_child_weight` → `subsample/colsample` → `reg_*` → 小 `eta` + 早停。网格爆炸时用随机搜索/贝叶斯。

---

#### Q7：因果推断、漏斗分析、AB Test、PSM

**A：**

| 方法     | 用途               | 核心思想                 |
| -------- | ------------------ | ------------------------ |
| 因果推断 | 回答「是否导致」   | 控制混淆，估计处理效应   |
| 漏斗分析 | 定位流失环节       | 分步转化率找瓶颈         |
| AB Test  | 评估策略增量       | 随机分流，对比实验/对照  |
| PSM      | 无法随机时的准实验 | 倾向得分匹配构造可比对照 |

**PSM 步骤：**

1. 用逻辑回归等估计倾向得分
2. 匹配（近邻/卡钳）处理组与对照组
3. 比较结果变量；检查平衡性与共同支撑

**怎么选：** 能随机就 AB；不能随机但有丰富协变量 → PSM/DID/合成控制；找问题在哪一步 → 漏斗。

---

## 核心知识点总结

### 1. SQL 重点

- 窗口函数（ROW_NUMBER / RANK / DENSE_RANK、累计求和）
- 留存（新用户 vs 活跃；补全 0 行）
- 连续日期（DATE_SUB + ROW_NUMBER）
- 字符串拆分（**CHAR_LENGTH**，不是 LENGTH）
- TopN 与去极值的并列语义

### 2. 统计学重点

- 中心极限定理 / 大数定律（会举业务例子）
- p 值正确解读（不是原假设概率）
- 第一类 / 第二类错误与 Power
- 报效应量与置信区间，不只报显著性

### 3. 机器学习重点

- Xgboost 原理与防过拟合 / 调参路径
- L1 / L2 / 弹性网
- SVM 最大间隔与核
- PCA 步骤与局限
- 多重共线性（VIF、业务例子）

### 4. 业务分析重点

- 口径定义 → 描述 → 相关 → 匹配/实验
- 指标拆解（GMV = 流量 × 转化 × 客单价）
- AB Test 与增量思维
- 漏斗 + 渠道 ROI（注意自然量混杂）

### 5. 大模型产品重点

- 训练流程（预训练 → SFT → 对齐）
- 数据质量与自动筛复杂度
- CoT 压缩：SFT 蒸馏 vs RL 长度惩罚
- NL2SQL 以执行准确率为主指标

---

## 附：SQL 易错对照表

| 点             | 错误写法                              | 正确写法                                    |
| -------------- | ------------------------------------- | ------------------------------------------- |
| 历史峰值       | `MAX(SUM(x)) OVER ... GROUP BY`     | 先窗口累计`SUM() OVER`，再 `MAX` 聚合   |
| 中文字符串长度 | `LENGTH`                            | `CHAR_LENGTH`                             |
| 0 留存不出行   | 内联只出有留存的行                    | 日期维表`CROSS JOIN` + `LEFT JOIN`      |
| 去掉最高最低   | 只用`ROW_NUMBER`（并列去 1 人）     | `salary > MIN AND salary < MAX`（整档）   |
| 除零           | `a / COUNT(*)`                      | `NULLIF(COUNT(*), 0)`                     |
| LTV 外推       | `rev * 365 / days` 且只统计付费用户 | 同期群比例/曲线拟合 +**含未付费用户** |

---

*祝面试顺利！*
